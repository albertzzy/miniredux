## a simple state manager highly inspired by redux(which is still being constructed)

	### regardless of tracking-difference / immutable recommanded

state
	dispatch(actionPayload,callback[prev,cur]) 
		reducer
	subscribe // global watch

combineReducer
	reducer managament

